# PS 26237 — Python dependencies
# Versions are the ones this prototype was built and tested against
# (Windows 11, Python 3.14.4). Install offline with:
#   pip install --no-index --find-links=wheels -r requirements.txt

liboqs-python==0.16.0     # ML-KEM-768 (FIPS 203) + ML-DSA-65 (FIPS 204)
cryptography==46.0.7      # AES-256-GCM, HKDF, scrypt key storage
numpy==2.4.4              # watermark maths
opencv-python==5.0.0.93   # DCT transform, image I/O
pymupdf==1.28.2           # PDF render / rebuild
Flask==3.1.3              # local operator web UI

# IMPORTANT: liboqs-python is only the Python binding. It needs the compiled
# liboqs C library (oqs.dll / liboqs.so) present on the machine. If it cannot
# find one, it tries to git-clone and build liboqs at import time, which fails
# on an air-gapped host. Ship a prebuilt library and set OQS_INSTALL_PATH.
# Build instructions: docs/DEPLOYMENT.md section 1.2.
#
# The Fabric ledger (ledger/fabric/) additionally needs Docker; its Go chaincode
# dependencies are vendored in ledger/fabric/chaincode/provenance/vendor, so it
# builds without network access.
