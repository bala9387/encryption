runtime: python311
instance_class: F2

entrypoint: gunicorn --bind :$PORT --workers 1 --threads 8 --timeout 300 wsgi:app

env_variables:
  PYTHONUNBUFFERED: "1"
  PS26237_HOME: "/tmp/ps26237_workspace"

handlers:
  - url: /.*
    script: auto
    secure: always
